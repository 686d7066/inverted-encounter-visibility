{
  "name": "encounter-visibility",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --experimental-transform-types scripts/build.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0",
    "vitest": "^4.0.8"
  }
}
